# 🚀 Быстрый старт для глобального доступа

## Вариант 1: Cloudflare Tunnel (Рекомендуется - 5 минут)

### Шаг 1: Установка
```bash
curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb -o cloudflared.deb
sudo dpkg -i cloudflared.deb
```

### Шаг 2: Авторизация
```bash
cloudflared tunnel login
```
Выберите ваш домен (или создайте аккаунт на cloudflare.com)

### Шаг 3: Быстрый запуск (для тестирования)
```bash
# В терминале 1: Запустите приложение
npm run dev

# В терминале 2: Запустите туннель
./scripts/start-tunnel-quick.sh 5173
```

### Шаг 4: Обновите .env
Скопируйте HTTPS URL из вывода туннеля и добавьте в `.env`:
```env
WEBAPP_URL=https://xxxxx-xxxxx.trycloudflare.com
```

### Шаг 5: Перезапустите приложение
Теперь Telegram WebApp будет работать!

---

## Вариант 2: Постоянный домен (Для продакшена)

### Создайте туннель:
```bash
cloudflared tunnel create battleship
```

### Настройте конфигурацию:
1. Скопируйте `cloudflared-config.example.yml` в `~/.cloudflared/config.yml`
2. Замените `YOUR_TUNNEL_ID` на ID вашего туннеля
3. Настройте hostname (ваш домен или workers.dev)

### Запустите туннель:
```bash
cloudflared tunnel run battleship
```

### Настройте автозапуск:
```bash
sudo systemctl enable cloudflared
sudo systemctl start cloudflared
```

---

## Важно! 🔒

1. **HTTPS обязателен** - Telegram WebApp работает только с HTTPS
2. **Порт 5173** - Frontend dev сервер (для продакшена используйте 3000)
3. **Переменная WEBAPP_URL** - должна начинаться с `https://`

---

## Troubleshooting

### Туннель не запускается
```bash
# Проверьте установку
cloudflared --version

# Проверьте авторизацию
cloudflared tunnel list
```

### URL меняется при перезапуске
Это нормально для быстрого режима. Для постоянного URL используйте вариант 2.

### Telegram не открывает WebApp
- Убедитесь, что URL начинается с `https://`
- Проверьте, что туннель работает: `curl https://your-url`
- Убедитесь, что приложение запущено на правильном порту

---

📖 **Полная документация:** [DEPLOYMENT.md](DEPLOYMENT.md)

